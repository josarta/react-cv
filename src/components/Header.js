import React from 'react';

const Header = props => {
    return(
        <div className="top-bar">
            <div className="top-bar-left">
                Matt Chapman
          </div>
        </div>
    );
}

export default Header;